<Window x:Class="PartAnalyzer.Views.FilterWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Filters and Presets" Width="1050" Height="680" WindowStartupLocation="CenterOwner">
<Grid Margin="14" IsEnabled="{Binding IsFilterInteractionEnabled, Mode=OneWay}"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="170"/></Grid.RowDefinitions>
    <DockPanel Margin="0,0,0,10"><TextBlock Text="Filter preset" VerticalAlignment="Center" Margin="0,0,8,0"/><ComboBox ItemsSource="{Binding PresetNames}" Text="{Binding PresetName, UpdateSourceTrigger=PropertyChanged}" IsEditable="True" Width="200"/><Button Content="Load" Click="LoadPreset_Click" Margin="8,0,0,0" Padding="10,4"/><Button Content="Save New" Click="SavePreset_Click" Margin="8,0,0,0" Padding="10,4"/><Button Content="Update" Click="UpdatePreset_Click" Margin="8,0,0,0" Padding="10,4"/><Button Content="Delete" Click="DeletePreset_Click" Margin="8,0,0,0" Padding="10,4"/><TextBlock Text="Some selected preset values are unavailable in this worksheet." Foreground="DarkOrange" Margin="18,0,0,0" VerticalAlignment="Center"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding HasUnavailableSelectedValues, Mode=OneWay}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></DockPanel>
    <ItemsControl Grid.Row="1" ItemsSource="{Binding ConstrainedFilters}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="3"/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Border BorderBrush="#DADDE1" BorderThickness="1" Margin="5" Padding="10"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="16"/><TextBox Grid.Row="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,8" ToolTip="Search visible values"/><ListBox Grid.Row="2" ItemsSource="{Binding Options}"><ListBox.ItemTemplate><DataTemplate><CheckBox Content="{Binding DisplayWithCount}" IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"/></DataTemplate></ListBox.ItemTemplate></ListBox><Button Grid.Row="3" Content="Clear selections" Click="ClearFilter_Click" Tag="{Binding}" Margin="0,8,0,0" Padding="8,4"/></Grid></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
    <Border Grid.Row="2" BorderBrush="#DADDE1" BorderThickness="1" Padding="10" Margin="5"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="250"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Manufacturer preview" FontWeight="SemiBold" FontSize="16"/><TextBlock Text="Complete eligible worksheet; trim-only identities." TextWrapping="Wrap" Margin="0,6,0,0"/></StackPanel><ListBox Grid.Column="1" ItemsSource="{Binding ManufacturerPreview}"><ListBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding DisplayWithCount}"/></DataTemplate></ListBox.ItemTemplate></ListBox></Grid></Border>
  </Grid>
</Window>
